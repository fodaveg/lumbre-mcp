node_modules/
dist.old/
dist.new/
.distcheck-tmp/
.DS_Store
*.log

# El repo es público y `.env.example` invita a crear `.env` con una clave de
# configuración: que ese fichero local nunca pueda acabar versionado.
.env
.env.*
!.env.example

# Worktrees y estado local de los agentes; nunca se versiona.
.claude/
